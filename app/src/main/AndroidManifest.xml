<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vinit.farmzone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectorActivity" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".SelectorActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".ProviderHomeActivity" />
        <activity android:name=".RenterHomeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity
            android:name=".AddEquipmentActivity"
            android:parentActivityName=".ProviderHomeActivity" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".ChatActivity"
            android:parentActivityName=".RenterHomeActivity" />
        <activity android:name=".SelectUserActivity" />
        <activity android:name=".ForgetpasswordActivity"></activity>
    </application>

</manifest>